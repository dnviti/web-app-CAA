<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Title}}</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/static/style/style.css">
    <script src="/static/script/auth.js"></script>
    <script src="/static/script/config.js"></script>
</head>
<body>
    <div class="login-container">
        <div class="login-content">
            <h3>Accesso Utente</h3>
            {{if .Error}}
            <div class="error-message">{{.Error}}</div>
            {{end}}
            {{if .Success}}
            <div class="success-message">{{.Success}}</div>
            {{end}}
            <div class="form-group">
                <label for="username">Username:</label>
                <input type="text" id="username" placeholder="Inserisci il tuo username" autocomplete="username" value="{{.FormData.Username}}">
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" placeholder="Inserisci la tua password" autocomplete="current-password">
            </div>
            <div class="modal-buttons">
                <button class="btn-primary" id="loginBtn">Accedi</button>
            </div>
            <div class="register-link">
                <p>Non hai un account? <a href="/register">Registrati</a></p>
            </div>
        </div>
    </div>
</body>
</html>
